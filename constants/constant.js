const pi = 3.1415;
let radius;
let area;

//radius = Number(radius)         //can do this as there is no radius currently

//area = Number(area)

radius = Number(window.prompt("Enter the radius"))


area = pi * radius * radius;

console.log(`The area of youe circle is ${area}`)